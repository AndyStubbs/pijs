title = "stopAudio"
category = "sound"
isScreen = false
plugin = "sound"
summary = "Stops audio from an audio pool or all audio pools."
description = """
Stops all playing instances in the specified audio pool, or stops all audio pools if audioId is null.
"""
example = """
let audio = $.loadAudio( "media/sound.wav", 1 );
$.ready( function () {
	$.playAudio( audio );
	setTimeout( function () {
		$.stopAudio( audio );
	}, 500 );
} );
"""

[[parameters]]
name = "audioId"
type = "string"
description = "Audio pool ID. If null, stops all audio pools."
optional = true

[[returns]]
type = "void"
description = "This function does not return a value."
