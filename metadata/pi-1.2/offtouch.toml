title = "offtouch"
category = "touch"
isScreen = true
summary = "Removes a touch event handler registered with the ontouch command."
description = """
Removes a touch event handler that was previously registered with the ontouch command. The function
reference and mode must exactly match the ones used when registering the handler with ontouch.

The callback receives (touchDataArray, customData) where touchDataArray is an array of touch data
objects containing x, y, id, lastX, lastY, action, and type properties.
"""
example = """
$.screen( "300x200" );\r
$.print( "Touch and drag to paint screen, touch up to stop." );\r
$.ontouch( "move", touchMove );\r
$.ontouch( "end", touchStop, true );\r
\r
// Touch move function receives touchDataArray and optional customData\r
function touchMove( touches, customData ) {\r
\t$.setPosPx( touches[ 0 ].x, touches[ 0 ].y );\r
\tvar pos = $.getPos();\r
\t$.setPos( pos.col, pos.row );\r
\t$.setColor( 8 );\r
\t$.print( "+", true );\r
}\r
\r
// Touch stop function receives touchDataArray and optional customData\r
function touchStop( touches, customData ) {\r
\t$.setColor( 14 );\r
\tvar pos = $.getPos();\r
\t$.setPos( pos.col - 4, pos.row );\r
\t$.print( "Stopped!", true );\r
\t$.offtouch( "move", touchMove );\r
}\r
"""

[[parameters]]
name = "mode"
type = "string"
description = "Event mode: 'start', 'end', or 'move'."

[[parameters]]
name = "fn"
type = "function"
signature = "( touchDataArray: Array<{ x: number, y: number, id: number, lastX: number, lastY: number, action: string, type: string }>, customData?: object ) => void"
description = "Callback function used when registering with ontouch."

[[returns]]
type = "void"
description = "This function does not return a value."
